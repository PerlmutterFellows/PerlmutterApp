<div class="d-flex justify-content-center">
  <div class="row w-75 m-4 d-flex justify-content-center">
    <%= render 'shared/card',
             header: get_event_html_header(@event),
             img: "",
             title: get_event_html_title(@event),
             body: get_event_html_body(@event, get_attending_counts(@event), true),
             footer: get_event_html_buttons(@event, true) %>
  </div>
</div>